//const router = require('express').Router();
//module.exports= router; 
//esto es una manera de escribir el enroutador pero la vamos a describir mejos como abajo:
const express = require('express');
const router = express.Router();


module.exports = router
